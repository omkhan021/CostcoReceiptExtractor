<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>

    <!-- Cold-start splash theme. MainActivity swaps to AppTheme in onCreate.
         The JS-rendered SplashScreen component takes over once RN mounts;
         the attributes below just keep the cold-start moment dark navy
         (matching the JS splash background) on every Android version. -->
    <style name="LaunchTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <!-- Android 12+ (API 31) system splash background -->
        <item name="android:windowSplashScreenBackground" tools:targetApi="s">@color/splash_background</item>
    </style>

</resources>
